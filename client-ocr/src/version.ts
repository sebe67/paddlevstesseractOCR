/**
 * Bumped on every push with a meaningful field-extraction change, so a bug report (or
 * the demo page) can be tied to the exact code that produced it, rather than "which
 * commit was this." Keep this in sync with package.json's "version" field - there's no
 * build step wiring the two together, so both need updating by hand together.
 */
export const LIBRARY_VERSION = "1.15.0";
